<Window x:Class="CoverageAnalyzerGUI.LoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HTTP Authentication" Height="350" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="HTTP Server Authentication Required" 
                   FontWeight="Bold" FontSize="14" Margin="0,0,0,15"/>
        
        <TextBlock Grid.Row="1" Name="ServerTextBlock" Text="Server: " 
                   Margin="0,0,0,20" TextWrapping="Wrap"/>
        
        <Label Grid.Row="2" Content="Username:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="3" Name="UsernameTextBox" Margin="0,0,0,15" Height="25" VerticalContentAlignment="Center"/>
        
        <Label Grid.Row="4" Content="Password:" Margin="0,0,0,5"/>
        <PasswordBox Grid.Row="5" Name="PasswordBox" Margin="0,0,0,15" Height="25" VerticalContentAlignment="Center"/>
        
        <!-- Spacer row -->
        <Border Grid.Row="6"/>
        
        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Name="OkButton" Content="OK" Width="75" Height="30" Margin="0,0,10,0" 
                    Click="OkButton_Click" IsDefault="True"/>
            <Button Name="CancelButton" Content="Cancel" Width="75" Height="30" 
                    Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>