<Window x:Class="CoverageAnalyzerGUI.HttpAuthDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HTTP Authentication Required" 
        Height="320" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="Manual">
    
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Icon and title -->
        <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,15">
            <Image Source="pack://application:,,,/System.Drawing;component/resources/SystemIcons_Shield.ico" 
                   Width="32" Height="32" 
                   VerticalAlignment="Center" 
                   Margin="0,0,10,0"/>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="HTTP Authentication Required" 
                           FontWeight="Bold" 
                           FontSize="14" 
                           Margin="5,0"/>
                <TextBlock Text="Please enter your credentials to access the file server:" 
                           FontSize="11" 
                           Foreground="Gray" 
                           Margin="5,0"/>
            </StackPanel>
        </StackPanel>

        <!-- Server URL -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Server:" FontWeight="Bold"/>
        <TextBlock Grid.Row="1" Grid.Column="1" Name="ServerLabel" Text="" Foreground="DarkBlue"/>

        <!-- Username -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Username:" FontWeight="Bold"/>
        <TextBox Grid.Row="2" Grid.Column="1" Name="UsernameTextBox" 
                 TabIndex="0"/>

        <!-- Password -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Password:" FontWeight="Bold"/>
        <PasswordBox Grid.Row="3" Grid.Column="1" Name="PasswordBox" 
                     TabIndex="1"/>

        <!-- Remember credentials option -->
        <CheckBox Grid.Row="4" Grid.ColumnSpan="2" 
                  Name="RememberCheckBox" 
                  Content="Keep authentication for this session" 
                  IsChecked="True"
                  Margin="5,10"/>

        <!-- Status message -->
        <TextBlock Grid.Row="5" Grid.ColumnSpan="2" 
                   Name="StatusMessage" 
                   Text="" 
                   Foreground="Red" 
                   Margin="5"
                   Visibility="Collapsed"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,15,0,0">
            <Button Name="OkButton" Content="OK" IsDefault="True" Click="OkButton_Click"/>
            <Button Name="CancelButton" Content="Cancel" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>